OPENAI_API_KEY=
COCKPIT_LLM_PROVIDER=
COCKPIT_CODEX_MODEL=
COCKPIT_CODEX_TIMEOUT_MS=
CEREBRAS_API_KEY=
CEREBRAS_MODEL=
# ─── Supabase: pick ONE target ────────────────────────────────────────
# Three isolated databases exist. NEVER point local dev or the self-dev
# swarm at the live (production) project — RLS does not protect you from
# your own authenticated deletes.
#   live (production, Vercel prod only): https://vipejmtmnqahjbwyezdj.supabase.co
#   preview / agents / non-prod  (use this one): values below
#   local supabase start:                http://127.0.0.1:54321
# Publishable keys are browser-safe by design (RLS enforces per-user access).
NEXT_PUBLIC_SUPABASE_URL=https://akjrmjyunxhnienqkoia.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_qhuZIR2So2UQFGFbXnn_LQ_L6bmUHpa

# ─── Plugin system (Phase 1) ──────────────────────────────────────────
# CSV of enabled plugin ids
COCKPIT_PLUGINS=vibe
# CSV of repo paths the Vibe plugin scans for lanes/*.json
COCKPIT_PLUGIN_VIBE_ROOTS=
